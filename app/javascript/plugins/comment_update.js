const commentUpdate = () => {
  // // add event Listener for clicking on buttons
  // const scores = document.querySelectorAll('.weighted-score');
  // const buttons = document.querySelectorAll('.btn-votes');

  // const sendAjax = (event) => {
  //   // Rails.ajax({
  //   //   url: "/books",
  //   //   type: "get",
  //   //   data: "",
  //   // })
  // }

  // buttons.forEach((button) => {
  //   button.addEventListener('click', (event) => {
  //     if (button.dataset.value == 'up') {
  //       // scores[button.dataset.id].innerHTML = parseInt(scores[button.dataset.id].innerHTML, 10) + 1;
  //       console.log(button.dataset.id.content)
  //     } else {
  //       scores[button.dataset.id].innerHTML = parseInt(scores[button.dataset.id].innerHTML, 10) - 1;
  //     }
  //     // doesn't work because we need to know if the user has already voted..
  //     sendAjax(event);
  //   })
  // })
  // // const listener = document.querySelector('.weighted-score');

}

export { commentUpdate };

